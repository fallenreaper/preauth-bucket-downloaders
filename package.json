{
  "name": "filetransfer",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/fallenreaper/preauth-bucket-downloaders#readme",
  "bugs": {
    "url": "https://github.com/fallenreaper/preauth-bucket-downloaders/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fallenreaper/preauth-bucket-downloaders.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test:aws": "dotenv-run -r ./.env -- pnpm --filter aws-sample test",
    "test:cloudflare": "dotenv-run -r ./.env -- pnpm --filter cloudflare-sample test",
    "format": "pnpm exec biome format --write",
    "lint": "pnpm exec biome lint",
    "lint:fix": "pnpm exec biome lint --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@dotenv-run/cli": "^1.3.6",
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
